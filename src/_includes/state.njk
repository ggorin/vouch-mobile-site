---
layout: base.njk
---

{% block schema %}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "LocalBusiness",
      "@id": "{{ site.url }}/#organization",
      "name": "{{ site.name }}",
      "url": "{{ site.url }}",
      "priceRange": "$30/month",
      "areaServed": {
        "@type": "State",
        "name": "{{ state.name }}",
        "containedInPlace": {
          "@type": "Country",
          "name": "United States"
        }
      }
    },
    {
      "@type": "Service",
      "serviceType": "Mobile Phone Service",
      "provider": {
        "@id": "{{ site.url }}/#organization"
      },
      "areaServed": {
        "@type": "State",
        "name": "{{ state.name }}",
        "containsPlace": [
          {% for city in state.cities %}
          {"@type": "City", "name": "{{ city.name }}"}{% if not loop.last %},{% endif %}
          {% endfor %}
        ]
      }
    },
    {
      "@type": "FAQPage",
      "mainEntity": [
        {% if state.faqs %}
        {% for faq in state.faqs %}
        {
          "@type": "Question",
          "name": "{{ faq.question }}",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "{{ faq.answer }}"
          }
        }{% if not loop.last %},{% endif %}
        {% endfor %}
        {% else %}
        {
          "@type": "Question",
          "name": "Does Vouch Mobile work in {{ state.name }}?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Yes! Vouch Mobile uses AT&T's network which provides excellent coverage across {{ state.name }} with {{ state.coverage }} population coverage."
          }
        },
        {
          "@type": "Question",
          "name": "Does Vouch Mobile require a credit check in {{ state.name }}?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "No! Vouch Mobile never requires a credit check. {{ state.name }} residents can sign up regardless of credit history."
          }
        },
        {
          "@type": "Question",
          "name": "Does Vouch Mobile have 5G in {{ state.name }}?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Yes! 5G coverage is available in major {{ state.name }} cities including {{ state.cities[0].name }}."
          }
        }
        {% endif %}
      ]
    },
    {
      "@type": "BreadcrumbList",
      "itemListElement": [
        {
          "@type": "ListItem",
          "position": 1,
          "name": "Home",
          "item": "{{ site.url }}"
        },
        {
          "@type": "ListItem",
          "position": 2,
          "name": "Coverage",
          "item": "{{ site.url }}/coverage/"
        },
        {
          "@type": "ListItem",
          "position": 3,
          "name": "{{ state.name }}"
        }
      ]
    }
  ]
}
</script>
{% endblock %}

<section class="page-header">
  <div class="container">
    <nav class="breadcrumb" aria-label="Breadcrumb">
      <a href="/">Home</a> &rsaquo;
      <a href="/coverage/">Coverage</a> &rsaquo;
      <span>{{ state.name }}</span>
    </nav>
    <h1>Vouch Mobile in {{ state.name }}: Unlimited Plans on AT&T's Network</h1>
    <p>Get unlimited talk, text & data in the {{ state.nickname }} for just $30/month. No credit check required.</p>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="content-grid">
      <div class="content-main">
        {% if state.hasImage %}
        {% set imgPath = "/images/coverage/" + state.slug + "." + (state.imageExtension | default('jpg')) %}
        {% image imgPath, "Vouch Mobile coverage in " + state.name, [400, 800, 1200], "(min-width: 1024px) 66vw, 100vw", { class: "hero-image", loading: "eager" } %}
        {% endif %}

        {# Key Facts Box - Enhanced for states with full data #}
        {% if state.population %}
        <div class="key-facts-box">
          <h2>{{ state.name }} at a Glance</h2>
          <div class="facts-grid">
            <div class="fact-item">
              <span class="fact-label">Population</span>
              <span class="fact-value">{{ state.population }}</span>
            </div>
            <div class="fact-item">
              <span class="fact-label">AT&T Coverage</span>
              <span class="fact-value">{{ state.coverage }}</span>
            </div>
            <div class="fact-item">
              <span class="fact-label">Avg. Phone Bill</span>
              <span class="fact-value">{{ state.avgPhoneBill }}/mo</span>
            </div>
            <div class="fact-item">
              <span class="fact-label">Vouch Price</span>
              <span class="fact-value highlight">$30/mo</span>
            </div>
            <div class="fact-item">
              <span class="fact-label">Annual Savings</span>
              <span class="fact-value highlight">{{ state.annualSavings }}</span>
            </div>
            <div class="fact-item">
              <span class="fact-label">Credit Check</span>
              <span class="fact-value highlight">None</span>
            </div>
          </div>
          {% if state.externalLinks %}
          <p class="facts-source">Data from <a href="{{ state.externalLinks[0].url }}" target="_blank" rel="noopener">{{ state.externalLinks[0].text }}</a></p>
          {% endif %}
        </div>
        {% else %}
        <div class="stats-grid">
          <div class="stat-card">
            <div class="stat-number">{{ state.coverage }}</div>
            <div class="stat-label">Population Coverage</div>
          </div>
          <div class="stat-card">
            <div class="stat-number">5G</div>
            <div class="stat-label">Network Available</div>
          </div>
          <div class="stat-card">
            <div class="stat-number">$30</div>
            <div class="stat-label">Per Month</div>
          </div>
          <div class="stat-card">
            <div class="stat-number">0</div>
            <div class="stat-label">Credit Check</div>
          </div>
        </div>
        {% endif %}

        {# Unique Hook - State-specific messaging #}
        {% if state.uniqueHook %}
        <div class="unique-hook">
          <p>{{ state.uniqueHook }}</p>
        </div>
        {% endif %}

        <h2>AT&T Network Coverage in {{ state.name }}</h2>
        <p>Vouch Mobile operates on AT&T's award-winning network, providing {{ state.name }} residents with reliable 5G and LTE coverage across the {{ state.nickname }}. With <strong>{{ state.coverage }} population coverage</strong>, you'll stay connected wherever you go.</p>

        {% if state.externalLinks %}
        <p>According to the <a href="{{ state.externalLinks[1].url }}" target="_blank" rel="noopener">{{ state.externalLinks[1].text }}</a>, AT&T provides extensive wireless coverage throughout {{ state.name }}, including urban centers and many rural areas.</p>
        {% endif %}

        <h2>Major {{ state.name }} Cities We Cover</h2>
        <p>Vouch Mobile provides excellent coverage in all major {{ state.name }} metropolitan areas:</p>

        <div class="city-grid">
          {% for city in state.cities %}
          <div class="city-item">
            <h3>{{ city.name }}</h3>
            <p>{{ city.description }}</p>
          </div>
          {% endfor %}
        </div>

        <h2>Why {{ state.name }} Residents Choose Vouch Mobile</h2>
        {% if state.whyVouch %}
        <ul class="feature-list">
          {% for reason in state.whyVouch %}
          <li>{{ reason }}</li>
          {% endfor %}
        </ul>
        {% else %}
        <ul class="feature-list">
          <li><strong>No Credit Check:</strong> Perfect for new {{ state.name }} residents, students, or anyone rebuilding credit.</li>
          <li><strong>AT&T Network Quality:</strong> Get reliable coverage at a fraction of the price.</li>
          <li><strong>Simple Pricing:</strong> Just $30/month for unlimited everything. No hidden fees.</li>
          <li><strong>5G Included:</strong> Access to AT&T's 5G network across major {{ state.name }} cities.</li>
          <li><strong>No Contracts:</strong> Month-to-month service. Cancel anytime.</li>
          <li><strong>Keep Your Number:</strong> Easily port your existing phone number.</li>
        </ul>
        {% endif %}

        {% if state.externalLinks[2] %}
        <p>The <a href="{{ state.externalLinks[2].url }}" target="_blank" rel="noopener">{{ state.externalLinks[2].text }}</a> shows that {{ state.name }} has a high {{ state.externalLinks[2].context }}, making affordable phone service even more valuable.</p>
        {% endif %}

        <h2>How to Get Started in {{ state.name }}</h2>
        <ol class="steps-list">
          <li><strong>Check Your Phone:</strong> Make sure your phone is unlocked and compatible. <a href="/guides/check-phone-unlocked/">Learn how to check</a>.</li>
          <li><strong>Choose Your Plan:</strong> Select our $30/month unlimited plan.</li>
          <li><strong>Order Your SIM:</strong> Free shipping to any {{ state.name }} address.</li>
          <li><strong>Activate Online:</strong> Simple activation takes just minutes. <a href="/guides/how-to-switch-carriers/">See our switching guide</a>.</li>
          <li><strong>Start Saving:</strong> Enjoy unlimited service across {{ state.name }}.</li>
        </ol>

        {# Internal Links Section #}
        <div class="related-content">
          <h3>Related Resources</h3>
          <ul>
            <li><a href="/guides/no-credit-check-phone-plans/">Complete Guide to No Credit Check Phone Plans</a></li>
            <li><a href="/guides/how-to-switch-carriers/">How to Switch Phone Carriers</a></li>
            <li><a href="/compare/vouch-vs-cricket/">Compare: Vouch vs Cricket Wireless</a></li>
          </ul>
        </div>
      </div>

      <aside class="content-sidebar">
        <div class="sidebar-card">
          <h3>{{ state.name }} Plan</h3>
          <div class="price-display">
            <span class="price">$30</span>
            <span class="period">/month</span>
          </div>
          <ul class="plan-features">
            <li>Unlimited Talk</li>
            <li>Unlimited Text</li>
            <li>Unlimited Data</li>
            <li>5G Access</li>
            <li>Mobile Hotspot</li>
            <li>No Credit Check</li>
          </ul>
          <a href="https://vouchmobile.com" class="btn btn-primary btn-block">Get Started</a>
        </div>

        <div class="sidebar-card">
          <h3>Other States</h3>
          <ul class="state-links">
            {% for s in states | slice(0, 4) %}
            {% if s.slug != state.slug %}
            <li><a href="/coverage/{{ s.slug }}/">{{ s.name }}</a></li>
            {% endif %}
            {% endfor %}
            <li><a href="/coverage/">View All States</a></li>
          </ul>
        </div>
      </aside>
    </div>
  </div>
</section>

<section class="section section-alt">
  <div class="container">
    <h2 class="section-title">{{ state.name }} Coverage FAQ</h2>
    <div class="faq-list">
      {% if state.faqs %}
      {% for faq in state.faqs %}
      <div class="faq-item">
        <button class="faq-question" aria-expanded="false">
          <span class="faq-question-text">{{ faq.question }}</span>
          <span class="faq-question-icon">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </span>
        </button>
        <div class="faq-answer">
          <p>{{ faq.answer }}</p>
        </div>
      </div>
      {% endfor %}
      {% else %}
      <div class="faq-item">
        <button class="faq-question" aria-expanded="false">
          <span class="faq-question-text">Does Vouch Mobile work in {{ state.name }}?</span>
          <span class="faq-question-icon">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </span>
        </button>
        <div class="faq-answer">
          <p>Yes! Vouch Mobile uses AT&T's network which provides excellent coverage across {{ state.name }} with {{ state.coverage }} population coverage.</p>
        </div>
      </div>
      <div class="faq-item">
        <button class="faq-question" aria-expanded="false">
          <span class="faq-question-text">Does Vouch Mobile require a credit check in {{ state.name }}?</span>
          <span class="faq-question-icon">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </span>
        </button>
        <div class="faq-answer">
          <p>No! Vouch Mobile never requires a credit check. {{ state.name }} residents can sign up regardless of credit history.</p>
        </div>
      </div>
      <div class="faq-item">
        <button class="faq-question" aria-expanded="false">
          <span class="faq-question-text">Does Vouch Mobile have 5G in {{ state.name }}?</span>
          <span class="faq-question-icon">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </span>
        </button>
        <div class="faq-answer">
          <p>Yes! 5G coverage is available in major {{ state.name }} cities including {{ state.cities[0].name }}{% if state.cities[1] %}, {{ state.cities[1].name }}{% endif %}{% if state.cities[2] %}, and {{ state.cities[2].name }}{% endif %}.</p>
        </div>
      </div>
      {% endif %}
    </div>
  </div>
</section>

<section class="section cta-section">
  <div class="container">
    <div class="cta-content">
      <h2>Ready to Switch in {{ state.name }}?</h2>
      <p>Join thousands of {{ state.name }} residents saving money with Vouch Mobile. No credit check, no contracts.</p>
      <a href="https://vouchmobile.com" class="btn btn-primary btn-large">Get Started Today</a>
    </div>
  </div>
</section>
