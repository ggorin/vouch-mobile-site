{#
  Savings Banner Component
  ========================
  A highlighted banner showing annual savings compared to average phone bills.

  Usage:
    {% from "components/savings-banner.njk" import savingsBanner %}
    {{ savingsBanner(location="California", annualSavings="$660", avgPhoneBill="$85") }}

  Parameters:
    - location: The location name (state, city, etc.)
    - annualSavings: Formatted annual savings amount (e.g., "$660")
    - avgPhoneBill: Formatted average monthly phone bill (e.g., "$85")
    - ctaLink: Optional CTA button link (default: "/plans/")
    - ctaText: Optional CTA button text (default: "See Plans")

  CSS Dependencies:
    - .savings-banner
    - .savings-content
    - .savings-icon
    - .savings-text
    - .savings-label
    - .savings-amount
    - .savings-detail
#}

{% macro savingsBanner(location, annualSavings, avgPhoneBill, ctaLink="/plans/", ctaText="See Plans") %}
{% if annualSavings %}
<section class="savings-banner">
  <div class="container">
    <div class="savings-content">
      <div class="savings-icon">
        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"></circle><path d="M16 8h-6a2 2 0 1 0 0 4h4a2 2 0 1 1 0 4H8"></path><path d="M12 18V6"></path></svg>
      </div>
      <div class="savings-text">
        <span class="savings-label">{{ location }} residents save up to</span>
        <span class="savings-amount">{{ annualSavings }}/year</span>
        <span class="savings-detail">compared to the average {{ avgPhoneBill }}/mo phone bill</span>
      </div>
      <a href="{{ ctaLink }}" class="btn btn-primary">{{ ctaText }}</a>
    </div>
  </div>
</section>
{% endif %}
{% endmacro %}

{#
  Inline Savings Highlight Component
  ==================================
  A smaller inline savings highlight for use within content sections.

  Usage:
    {% from "components/savings-banner.njk" import savingsHighlight %}
    {{ savingsHighlight(annualSavings="$660") }}

  Parameters:
    - annualSavings: Formatted annual savings amount
    - prefix: Text before the amount (default: "Save up to")
    - suffix: Text after the amount (default: "/year")
#}

{% macro savingsHighlight(annualSavings, prefix="Save up to", suffix="/year") %}
{% if annualSavings %}
<span class="savings-highlight">
  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"></circle><path d="M16 8h-6a2 2 0 1 0 0 4h4a2 2 0 1 1 0 4H8"></path><path d="M12 18V6"></path></svg>
  {{ prefix }} <strong>{{ annualSavings }}</strong>{{ suffix }}
</span>
{% endif %}
{% endmacro %}
